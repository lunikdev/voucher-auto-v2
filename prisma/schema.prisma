// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Login {
  id             Int       @id @default(autoincrement())
  username       String    @unique
  password       String
  active         Boolean   @default(true)
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
  users          User[]    // Relação 1:N - um login pode ter vários usuários
}

model User {
  id        Int      @id @default(autoincrement())
  name      String
  email     String
  phone     String
  ip        String   // Armazena o IP do usuário
  mac       String   // Armazena o MAC do usuário
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  login     Login    @relation(fields: [loginId], references: [id])
  loginId   Int      // Chave estrangeira para o login
}
